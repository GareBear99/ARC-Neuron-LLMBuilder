name: Ingest operator reviews

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-placeholder:
    name: Manual placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled live ingest
        run: |
          echo "Live scheduled ingest is disabled for public repo stability."
          echo "Use manual workflow_dispatch only after required secrets and inputs are configured."
